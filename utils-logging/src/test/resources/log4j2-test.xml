<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO" name="XMLConfig" packages="com.asynchrony">
    <Appenders>
        <List name="LIST" />
        <Console name="STDOUT">
            <PatternLayout pattern="%date{ISO8601} %nano (%thread) [%highlight{%-5level}] %logger{36} %msg — %ex{short.message}%n%throwable{none}"/>
        </Console>
    </Appenders>

    <Loggers>
        <Root level="TRACE">
            <AppenderRef ref="STDOUT" />
            <AppenderRef ref="LIST" />
        </Root>
    </Loggers>
</Configuration>